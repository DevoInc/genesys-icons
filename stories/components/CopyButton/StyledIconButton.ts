import styled from 'styled-components';

import { StyledButton } from '../DownloadSVGButton/StyledButton';

export const StyledIconButton = styled(StyledButton)`
  width: 24px;
  height: 24px;
  padding: 0;

  > svg {
    margin-right: 0;
  }
`;
