import styled from 'styled-components';

export const StyledText = styled.p`
  align-items: center;
  display: flex;
  gap: 8px;
  margin: 0;
  font-size: 13px;
  white-space: nowrap;
  font-weight: bold;
`;
