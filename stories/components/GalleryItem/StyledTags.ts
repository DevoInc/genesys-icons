import styled from 'styled-components';

export const StyledTags = styled.div`
  flex-wrap: wrap;
  column-gap: 8px;
  row-gap: 2px;
  display: flex;
  font-size: 13px;

  span {
    white-space: nowrap;
  }
`;
